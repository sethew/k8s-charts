apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bitcoind.fullname" . }}-auth
  labels:
    {{- include "bitcoind.labels" . | nindent 4 }}
type: Opaque
data:
  BTC_RPCUSER: {{ .Values.bitcoind.rpc.user | b64enc }}
  BTC_RPCPASSWORD: {{ include "bitcoind.rpcPassword" . | b64enc }}
